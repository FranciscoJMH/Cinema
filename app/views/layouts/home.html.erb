

  <!-- Page Content -->
  <div class="container">

    <div class="row">

      <div class="col-lg-3">

        <h1 class="my-4">Categorías</h1>
        <div class="list-group">
          <% @categories.each do |category| %>
            <a href="#" class="list-group-item"><%= category %></a>
          <% end %>
        </div>

      </div>
      <!-- /.col-lg-3 -->

      <div class="col-lg-9">

        <!-- Carousel row -->
        <div class="row">
          <div class="col col-md-10 offset-md-1 col-lg-8 offset-lg-2">
            <!-- Carousel -->
            <div id="carousel-example" class="carousel slide">
              <ol class="carousel-indicators">
                <li data-target="#carousel-example" data-slide-to="0" class="active"></li>
                <li data-target="#carousel-example" data-slide-to="1"></li>
                <li data-target="#carousel-example" data-slide-to="2"></li>
              </ol>
              <div class="carousel-inner">

                <div class="carousel-item active">
                  <div class="embed-responsive embed-responsive-16by9">
                    <iframe class="embed-responsive-item" src=<%= @movies.first.trailer %> allowfullscreen></iframe>
                  </div>
                </div>

                <% @movies.first(3).drop(1).each do |movie| %>
                  <div class="carousel-item">
                    <div class="embed-responsive embed-responsive-16by9">
                      <iframe class="embed-responsive-item" src=<%= movie.trailer %> allowfullscreen></iframe>
                    </div>
                  </div>
                <% end %>

              </div>
              <a class="carousel-control-prev" href="#carousel-example" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
              </a>
              <a class="carousel-control-next" href="#carousel-example" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
              </a>
            </div>
            <!-- End carousel -->
          </div>
        </div>
        <!-- End carousel row -->

        <div class="row">
          <% @movies.each do |movie| %>
            <div class="col-lg-4 col-md-6 mb-4">
              <div class="card h-100">
                <%= link_to image_tag(movie.cover, alt: "", height: "300", width: "250"), movie, class: "card-img-top" %>
                <div class="card-body">
                  <h4 class="card-title">
                    <%= link_to movie.title, movie %>
                  </h4>
                  <h5><%= movie.year.year %></h5>
                  <p class="card-text"><%= movie.description %></p>
                </div>
                <div class="card-footer">
                  <small class="text-muted">
                    <% (movie.rating).times do %>
                        &#9733;
                    <% end %>
                    <% (5 - movie.rating).times do %>
                        &#9734;
                    <% end %>
                  </small>
                </div>
              </div>
            </div>
          <% end %>
        </div>
        <!-- /.row -->

      </div>
      <!-- /.col-lg-9 -->

    </div>
    <!-- /.row -->

  </div>
  <!-- /.container -->


